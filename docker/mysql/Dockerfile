# MariaDB matches local GoldWitch better than MySQL 8 auth/SQL quirks.
# Persistent disk must mount at /var/lib/mysql (MariaDB uses the same path).
FROM mariadb:10.11

ENV MARIADB_AUTO_UPGRADE=1

EXPOSE 3306
